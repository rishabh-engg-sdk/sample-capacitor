import {CapacitorConfig} from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.capacitor.example',
  appName: 'sample-capacitor',
  webDir: 'dist/sample-capacitor',
  bundledWebRuntime: false
};

export default config;
